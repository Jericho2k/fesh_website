# FESH Landing Page

Лендинг-страница для приложения FESH - Fashion Discovery App.

## 🚀 Быстрый старт

### Локальный запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите приложение:
```bash
python3 app.py
```

3. Откройте в браузере: http://localhost:8000

## 📦 Деплой на хостинг

Проект готов к деплою на следующие платформы:

### 1. Render (Рекомендуется - Бесплатный)

**Шаги:**
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python3 app.py`
   - **Environment**: Python 3
5. Нажмите "Create Web Service"

**Или используйте render.yaml:**
- Просто подключите репозиторий, Render автоматически обнаружит `render.yaml`

**Бесплатный план:** Приложение "засыпает" после 15 минут бездействия, но это бесплатно.

---

### 2. Railway

**Шаги:**
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит Python проект и использует `railway.json`
5. Деплой произойдет автоматически

**Бесплатный план:** $5 кредитов в месяц (обычно хватает для небольшого проекта)

---

### 3. Fly.io

**Шаги:**
1. Установите Fly CLI: `curl -L https://fly.io/install.sh | sh`
2. Зарегистрируйтесь: `fly auth signup`
3. В директории проекта выполните:
```bash
fly launch
```
4. Fly.io автоматически обнаружит `fly.toml` и задеплоит приложение

**Бесплатный план:** 3 shared-cpu-1x VMs с 256MB RAM

---

### 4. PythonAnywhere

**Шаги:**
1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Загрузите файлы через веб-интерфейс или Git
3. Создайте новое Web App
4. Укажите путь к `app.py`
5. Настройте WSGI файл для Flask

**Бесплатный план:** Ограниченный, но подходит для тестирования

---

### 5. Heroku

**Шаги:**
1. Установите Heroku CLI
2. Зарегистрируйтесь на [heroku.com](https://heroku.com)
3. В директории проекта:
```bash
heroku create your-app-name
git push heroku main
```

**Примечание:** Heroku больше не предлагает бесплатный план, но есть платные варианты от $5/месяц

---

### 6. DigitalOcean App Platform

**Шаги:**
1. Зарегистрируйтесь на [digitalocean.com](https://www.digitalocean.com)
2. Создайте новый App
3. Подключите GitHub репозиторий
4. DigitalOcean автоматически определит Python проект

**Планы:** От $5/месяц

---

## 🔧 Переменные окружения

При необходимости можно настроить:

- `PORT` - Порт для запуска (по умолчанию 8000)
- `FLASK_ENV` - Режим работы (`development` или `production`)

## 📁 Структура проекта

```
fesh_landing/
├── app.py                 # Главный файл Flask приложения
├── requirements.txt       # Python зависимости
├── Procfile              # Для Heroku
├── render.yaml           # Конфигурация для Render
├── railway.json          # Конфигурация для Railway
├── fly.toml              # Конфигурация для Fly.io
├── Dockerfile            # Для Docker деплоя
├── templates/            # HTML шаблоны
│   ├── index.html
│   ├── contact.html
│   └── stats.html
└── static/               # Статические файлы
    ├── css/
    │   └── style.css
    └── js/
        └── script.js
```

## 📝 Роуты

- `/` - Главная страница
- `/contact` - Страница контактов
- `/stats101` - Статистика посещений
- `/track/<action>` - API для отслеживания действий (POST)

## 🐳 Docker деплой

Если хотите использовать Docker:

```bash
docker build -t fesh-landing .
docker run -p 8000:8080 -e PORT=8080 fesh-landing
```

## ⚠️ Важные замечания

1. **Файл stats.log** создается автоматически и хранит статистику. На некоторых платформах файлы могут удаляться при перезапуске.

2. Для production рекомендуется использовать базу данных вместо файлов для хранения статистики.

3. Убедитесь, что все зависимости указаны в `requirements.txt`

## 📧 Контакты

- Email: support@fesh.world
- Telegram: @FESH_support
